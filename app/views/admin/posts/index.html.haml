%h1 Posts
%table.table
  %thead
    %tr
      %th Title
      %th Feed
      %th Author
      %th Slug
      %th Body
      %th{colspan: "3"}
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= post.feed.title
        %td= post.author.full_name
        %td= post.slug
        %td= post.body.truncate(30)
        %td= link_to 'Show', [:admin, post]
        %td= link_to 'Edit', edit_admin_post_path(post)
        %td= link_to 'Destroy', [:admin, post], method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Post', new_admin_post_path
